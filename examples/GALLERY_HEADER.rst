Executable examples
===================

These examples are executed during the strict Sphinx documentation build. They
are intentionally small, deterministic scientific workflows rather than a
second test suite. Each example records its model assumptions, keeps trajectory
boundaries explicit, and uses public ComplexTorch APIs.

The generated pages also provide downloadable Python source and Jupyter
notebooks through Sphinx-Gallery.
